version: 2

before:
  hooks:
    - go mod tidy

builds:
  - binary: tasknotes-cli
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/thepsadmin/tasknotes-cli/cmd.Version={{.Version}}
      - -X github.com/thepsadmin/tasknotes-cli/cmd.CommitSHA={{.ShortCommit}}

archives:
  - formats:
      - tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: 'checksums.txt'

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'

brews:
  - repository:
      owner: thepsadmin
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/thepsadmin/tasknotes-cli"
    description: "CLI for managing TaskNotes tasks in an Obsidian vault"
    license: "MIT"
    test: |
      system "#{bin}/tasknotes-cli", "version"
    install: |
      bin.install "tasknotes-cli"
